{
  "name": "kreatbio-client-portal-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloud Run backend for the KreatBio client bioinformatics portal.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check quota-store.js && node --check quota-admin.js",
    "quota": "node quota-admin.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google-cloud/storage": "^7.16.0",
    "better-sqlite3": "^12.4.1",
    "express": "^4.19.2"
  }
}
